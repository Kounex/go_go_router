import 'package:flutter/material.dart';

import '../../../../../shared/widgets/base/web_scaffold.dart';

class GamesDetailView extends StatelessWidget {
  const GamesDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return WebScaffold(
      appBar: AppBar(
        title: Text('Detail'),
      ),
      body: SizedBox(),
    );
  }
}
